# 06_media_player_app.dz — Multimedia Video & Audio Player in Djazair Qt
use qt

let app = new qt.QApplication()

let window = new qt.QMainWindow()
window.setTitle("Djazair Media Player")
window.resize(800, 500)

let theme = new qt.Theme()
theme.applyDark(window)

let videoWidget = new qt.QVideoWidget()
let player = new qt.QMediaPlayer()
player.setVideoOutput(videoWidget)

let vbox = new qt.QVBoxLayout()
vbox.addWidget(videoWidget)

let hbox = new qt.QHBoxLayout()
let btnPlay = new qt.QPushButton("Play")
let btnPause = new qt.QPushButton("Pause")
let btnStop = new qt.QPushButton("Stop")

hbox.addWidget(btnPlay)
hbox.addWidget(btnPause)
hbox.addWidget(btnStop)

let ctrlContainer = new qt.QWidget()
ctrlContainer.setLayout(hbox)

vbox.addWidget(ctrlContainer)

let mainContainer = new qt.QWidget()
mainContainer.setLayout(vbox)

window.setCentralWidget(mainContainer)
window.show()

app.exec()


