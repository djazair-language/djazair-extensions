# 01_hello_qt.dz — Hello World GUI Window in Djazair Qt
use qt

let app = new qt.QApplication()

let window = new qt.QMainWindow()
window.setWindowTitle("Djazair Qt — Hello World")
window.resize(500, 300)

let label = new qt.QLabel("Welcome to Djazair Enterprise Qt!")
let btn = new qt.QPushButton("Click Me")

let vbox = new qt.QVBoxLayout()
vbox.setMargins(20, 20, 20, 20)
vbox.setSpacing(10)
vbox.addWidget(label)
vbox.addWidget(btn)

let container = new qt.QWidget()
container.setLayout(vbox)

let theme = new qt.Theme()
theme.applyLight(window)

window.setCentralWidget(container)
window.show()

app.exec()


