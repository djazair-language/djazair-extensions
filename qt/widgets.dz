# widgets.dz — Root Module Alias
import "widgets/base.dz" as baseMod
import "widgets/controls.dz" as ctrlMod
import "widgets/inputs.dz" as inputMod
import "widgets/containers.dz" as containerMod
import "widgets/views.dz" as viewMod

let Widget      = baseMod.Widget

let Button      = ctrlMod.Button
let Label       = ctrlMod.Label
let CheckBox    = ctrlMod.CheckBox
let RadioButton = ctrlMod.RadioButton
let ProgressBar = ctrlMod.ProgressBar
let Slider      = ctrlMod.Slider
let SpinBox     = ctrlMod.SpinBox

let LineEdit    = inputMod.LineEdit
let TextEdit    = inputMod.TextEdit
let ComboBox    = inputMod.ComboBox

let TabWidget    = containerMod.TabWidget
let StackedWidget= containerMod.StackedWidget
let GroupBox     = containerMod.GroupBox
let ScrollArea   = containerMod.ScrollArea
let Splitter     = containerMod.Splitter

let ListWidget  = viewMod.ListWidget
let TreeWidget  = viewMod.TreeWidget
let TreeItem    = viewMod.TreeItem
let TableWidget = viewMod.TableWidget
