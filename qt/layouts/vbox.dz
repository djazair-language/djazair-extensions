# layouts/vbox.dz — Vertical Layout Manager
let _qt = __dynamic("qt")

class QVBoxLayout
    init(parent = Null)
        let parentHandle = Null
        if parent != Null
            parentHandle = parent.handle
        end
        self.handle = _qt.vboxCreate(parentHandle)
    end

    addWidget(widget)
        _qt.layoutAddWidget(self.handle, widget.handle)
    end

    addLayout(childLayout)
        _qt.layoutAddLayout(self.handle, childLayout.handle)
    end

    setMargins(left, top, right, bottom)
        _qt.layoutSetMargins(self.handle, left, top, right, bottom)
    end

    setSpacing(spacing)
        _qt.layoutSetSpacing(self.handle, spacing)
    end
end



