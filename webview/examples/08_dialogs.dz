# =============================================================================
# Project:      Djazair WebView Desktop Application Framework
# File:         examples/08_dialogs.dz
# Description:  Native Platform File, Folder, Message, and Color Dialog Pickers
# Author:       Harizi Riyadh (hariziriyadh@gmail.com)
# Copyright:    (c) 2026 Harizi Riyadh / Djazair Language Project. All rights reserved.
# License:      MIT License
# =============================================================================

use webview

# 1. Standard Informational Message Box
let result = webview.showMessageBox({
    "type": "info",
    "title": "Information",
    "message": "File saved successfully.",
    "detail": "The document was exported.",
    "buttons": "ok",
    "defaultId": 0,
    "cancelId": 0
})

# Open File (single)
let path = webview.showOpenFileDialog({
    "title": "Select a File",
    "defaultPath": "",
    "filters": [
        {"name": "Documents", "extensions": ["txt", "json", "md"]},
        {"name": "All Files", "extensions": ["*"]}
    ],
    "multiSelections": False,
    "properties": []
})

# Open File (multi)
let paths = webview.showOpenFileDialog({
    "title": "Select Multiple Files",
    "multiSelections": True,
    "filters": [{"name": "All Files", "extensions": ["*"]}]
})

# Save File
let path2 = webview.showSaveFileDialog({
    "title": "Save As",
    "defaultPath": "output.json",
    "filters": [
        {"name": "JSON", "extensions": ["json"]},
        {"name": "Text", "extensions": ["txt"]}
    ]
})

# Folder Picker
let folder = webview.showOpenFolderDialog({
    "title": "Select a Folder",
    "defaultPath": ""
})

# Color Picker
let color = webview.showColorPicker({
    "title": "Pick a Color"
})
