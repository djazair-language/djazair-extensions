# ═══════════════════════════════════════════════════════════════════════════════
# engine.dz — Qalam Engine & View Compatibility Layer
# ═══════════════════════════════════════════════════════════════════════════════
# Re-exports the modular View orchestrator to maintain seamless backward
# compatibility with existing applications and framework adapters (e.g. Kasbah).

import "view.dz" as viewMod
import "escaper.dz" as escaperMod

let View = viewMod.View
let view = viewMod.View
let escapeHtml = escaperMod.escapeHtml
